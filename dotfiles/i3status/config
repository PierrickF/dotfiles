general {
        colors = true
        interval = 1
}

order += "battery all"
order += "tztime local"

battery all {
        format = "%status %remaining"
}

tztime local {
        format = "%H:%M"
}
